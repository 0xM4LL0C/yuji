use "std/core"
use "std/io"

let age_str = input("Enter your age: ")

if not(age_str) {
    panic("Age cannot be empty")
}

let age = to_number(age_str)

if age < 0 {
    panic("Age cannot be negative")
}

if age >= 18 {
    println("You are an adult")
} else {
    println("You are a minor")
}
