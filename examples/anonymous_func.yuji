use "std/io"
use "std/array"

fn map(arr, func) {
    let result = []

    let i = 0
    while i < len(arr) {
        push(result, func(arr[i]))
        i += 1
    }

    return result
}

let result = map([1, 2, 3], fn(x) { x + 1 })
println(format("result: {}", result))
